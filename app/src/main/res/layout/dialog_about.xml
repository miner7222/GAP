<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="28dp"
    android:paddingEnd="24dp"
    android:paddingBottom="8dp">

    <ImageView
        android:id="@+id/aboutAppIcon"
        android:layout_width="76dp"
        android:layout_height="76dp"
        android:contentDescription="@null"
        android:src="@mipmap/ic_launcher_round" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/aboutAppName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="@string/app_name"
        android:textAppearance="@style/TextAppearance.GAP.AboutAppName" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/aboutVersion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.GAP.AboutVersion" />

    <ImageButton
        android:id="@+id/githubButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/bg_about_github_button"
        android:contentDescription="@string/github_link"
        android:minWidth="48dp"
        android:minHeight="48dp"
        android:padding="16dp"
        android:scaleType="center"
        android:src="@drawable/ic_github_24"
        app:tint="?attr/colorOnSecondaryContainer" />
</LinearLayout>
